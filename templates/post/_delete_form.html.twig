<form method="post" action="{{ path('post_delete', {'id': post.id}) }}" onsubmit="return confirm('Voulez vous vraiment supprimer ce post? Cette action est irréversible...');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ post.id) }}">
    <button class="btn  btn-success posBD"><i class="fas fa-trash"></i> Supprimer</button>
</form>
