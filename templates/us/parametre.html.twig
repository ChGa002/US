{% extends 'templateBase.html.twig' %}

{% block titleOnglet %}US - Paramètre{% endblock %}

{% block titlePage %} Paramètre : {{app.user.pseudo}} {% endblock %}

{% block contenuCentral %}

<div class="container-fluid">
	<div class="row">
		<div class="col-md-7">
            <ul class="list-group" style="text-align: center;">
                <a href="#switchPseudoModal" data-toggle='modal' data-target="#switchPseudoModal"> <li class="list-group-item"> Changer mon pseudo</li></a>
                <a href=""><li class="list-group-item">Changer mon mot de passe</li></a>
                <a href=""><li class="list-group-item">Supprimer mon compte</li></a>
            </ul>
		</div>
		<div class="col-md-5">
            <div class="text-center">
                <img src="{{ asset('/img/us_logo.png') }}" alt="" height="150px" width="150px">
            </div>
		</div>
	</div>
</div>

<div class="modal fade" id="switchPseudoModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel"
aria-hidden="true">
<div class="modal-dialog" role="document">
    <div class="modal-content">
        <div class="modal-header">
            {{ form_start(changerPseudoForm)}}
            <h5 class="modal-title" id="exampleModalLabel">Changement de pseudo :</h5>
            <button class="close" type="button" data-dismiss="modal" aria-label="Close">
                <span aria-hidden="true">×</span>
            </button>
        </div>
        <div class="modal-body">
            {{ form_row(changerPseudoForm.pseudo) }}
        </div>
        <div class="modal-footer">
            <button class="btn btn-secondary" type="button" data-dismiss="modal">Annuler</button>
            <button class="btn btn-secondary" type="submit" href="{{path('us_changePseudo')}}">Enregistrer</input>
                {{ form_end(changerPseudoForm)}}
        </div>
    </div>
</div>
</div>

{% endblock %}

{% block boxDroit %} {% endblock %}

{% block barreRecherche %} {% endblock %}

{% block Javascprit %}
    <script>
        const darkMode = document.getElementById('dark-mode');
        darkMode.addEventListener('change', () => {
        document.body.classList.toggle('dark');
        });
    </script>
{% endblock %}