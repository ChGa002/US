<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="Template personalisé pour la partie admin importation">
    <meta name="author" content="Ewen Blandin">
    <link rel="shortcut icon" href="{{ asset('/img/us_logo.png') }}"/>
    <title>US - Admin - Importation  Utilisateur</title>

    <!-- Custom fonts for this template-->
    <link href="vendor/fontawesome-free/css/all.min.css" rel="stylesheet" type="text/css">
    <link href="https://fonts.googleapis.com/css?family=Nunito:200,200i,300,300i,400,400i,600,600i,700,700i,800,800i,900,900i" rel="stylesheet">

    <!-- Custom styles for this template-->
    <link href="{{asset("css/sb-admin-2.css")}}" rel="stylesheet">
    <link rel="stylesheet" href="{{asset("/css/styleAdd.css")}}">
</head>

<body style="background:var(--deep);">

    <div class="container">
        <div class="row justify-content-center">
            <div class="col-xl-8 col-lg-10 col-md-9">
                <div class="card o-hidden border-0 shadow-lg my-5" style="background:var(--brique);">
                    <div class="card-body p-1">
                        <!-- Nested Row within Card Body -->
                        <div class="row justify-content-center">
                            <div class="col-lg-6">
                                {# <div class="p-5"> #}
                                        {# Placement du formulaire #}
                                        <h1 class="colorCloud">Importer vos utilisateurs</h1>
                                        <h2 class="colorCloud"><i>(Utilisez un fichier csv)</i></h2><br>
                                        <form action="{{ path('us_importation_upload') }}" enctype="multipart/form-data" method="POST" class="form-group align-self-center">
                                            <input type="hidden" name="MAX_FILE_SIZE" value="100000" class="">
                                            <input type="file" name="upload" id="" accept=".csv" class="form-control"><br>
                                            <input type="submit" name="send" class="btn btn-success" value="Importer"></input>
                                        </form>
                                        <a href=" {{ path('us_admin_dash') }}"><button class="btn btn-danger " style="position: absolute; right:5px; bottom:20px;">Retour</button></a>
                                    {# </div> #}
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap core JavaScript-->
    <script src="vendor/jquery/jquery.min.js"></script>
    <script src="vendor/bootstrap/js/bootstrap.bundle.min.js"></script>

    <!-- Core plugin JavaScript-->
    <script src="vendor/jquery-easing/jquery.easing.min.js"></script>

    <!-- Custom scripts for all pages-->
    <script src="js/sb-admin-2.min.js"></script>

</body>

</html>
